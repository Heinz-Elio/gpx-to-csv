# gpx-to-csv
Convert gpx file to csv for convenient hiking plan generation.
Require gpx track file with all waypoints named(including start point and end point), waypoint name can be in Chinese or English(other should be fine but not tested).
